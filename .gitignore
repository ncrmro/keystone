# Nix build outputs
result
result-*
installer-iso

# Nix development
.direnv/

# ISO files (they are large)
*.iso

# Editor / AI tool files
.vscode/
.gemini/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Temporary files
tmp/
temp/
.cache/
.nix-store/
.nixos-test-history

# VM artifacts
vms/*/disk.qcow2
vms/*/OVMF_VARS.fd
vms/*/*.pid
vms/*/*.ports
vms/*/*.log
vms/*/*.socket

# build-vm artifacts (nixos-rebuild build-vm)
.build-vm/
build-vm-*.pid
keystone-buildvm-*.qcow2
build-vm-*.qcow2

# VM configuration (keep .example files tracked)
vms/*.conf

# VM test artifacts (generated by test-deployment script)
vms/test-server/ssh-keys/
vms/test-server/extra-files/

.worktrees
worktree
.claude/worktrees/
__pycache__/

# Node.js (for keystone-installer-ui)
node_modules/
dist/
*.log

# Rust artifacts
target/

# VM screenshots for debugging
screenshots/
*.sock

# DeepWork runtime artifacts (jobs/ is tracked)
.deepwork/sessions/
.deepwork/tmp/

# TUI screenshots (removed from history)
assets/tui/
