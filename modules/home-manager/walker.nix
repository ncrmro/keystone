{
  programs.walker = {
    enable = true;
    
    runAsService = true;
    
    # Basic configuration - walker will use defaults
    # The package provides a complete application launcher for Hyprland
  };
}