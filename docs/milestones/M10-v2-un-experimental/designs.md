# Designs — v2 Un-experimental

> Mockups or surface references for v2. Stub for now.

v2 is primarily a graduation milestone — the operator-visible surfaces are
already designed in their experimental form. Capture screenshots or schema
diffs here once each candidate is approved for graduation:

- `keystone.notes` — notebook layout, sync flow, walker entry
- `archetypes.yaml` — composition diagram and example
- `projects.yaml` — expanded schema (fields added in v2)
