# Designs — v1 Stabilization

> Mockups, surfaces, or visual references for v1. Stub OK if the milestone is
> stability-only.

v1 is primarily stabilization — there is no new product surface to mock. The
two operator-visible surfaces being hardened are:

- **Walker `ks update` entry** — the click-to-update flow on workstation /
  laptop. Screenshots live in [`docs/desktop/walker.md`](../../desktop/walker.md)
  once recorded.
- **Verification matrix on each host** — see the table in
  [`tracker.md`](tracker.md) #verification-matrix.

Add screenshots here if v1.0.0 wants a visual in the release notes.
