interface:
  display_name: "Start the sweng/fix DeepWork workflow to fix a bug or issue"
  short_description: "Start the sweng/fix DeepWork workflow to fix a bug or issue"

dependencies:
  tools:
    - type: "mcp"
      value: "deepwork"
      description: "DeepWork MCP server"
