interface:
  display_name: "Start the keystone_system/update DeepWork workflow to deploy pending keystone changes to the fleet"
  short_description: "Start the keystone_system/update DeepWork workflow to deploy pending keystone changes to the fleet"

dependencies:
  tools:
    - type: "mcp"
      value: "deepwork"
      description: "DeepWork MCP server"
