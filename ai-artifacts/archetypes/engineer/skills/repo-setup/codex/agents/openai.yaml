interface:
  display_name: "Start the repo/setup DeepWork workflow to make a repo ready for work"
  short_description: "Start the repo/setup DeepWork workflow to make a repo ready for work"

dependencies:
  tools:
    - type: "mcp"
      value: "deepwork"
      description: "DeepWork MCP server"
