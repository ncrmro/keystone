interface:
  display_name: "Start the research/quick DeepWork workflow for a fast, local-only research summary"
  short_description: "Start the research/quick DeepWork workflow for a fast, local-only research summary"

dependencies:
  tools:
    - type: "mcp"
      value: "deepwork"
      description: "DeepWork MCP server"
