interface:
  display_name: "Onboard a new project interactively"
  short_description: "Onboard a new project interactively"

dependencies:
  tools:
    - type: "mcp"
      value: "deepwork"
      description: "DeepWork MCP server"
