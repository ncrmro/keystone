interface:
  display_name: "Start the agent_builder/doctor DeepWork workflow to diagnose agent health"
  short_description: "Start the agent_builder/doctor DeepWork workflow to diagnose agent health"

dependencies:
  tools:
    - type: "mcp"
      value: "deepwork"
      description: "DeepWork MCP server"
