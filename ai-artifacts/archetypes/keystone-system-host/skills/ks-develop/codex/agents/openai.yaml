interface:
  display_name: "Start the keystone_system/develop DeepWork workflow"
  short_description: "Start the keystone_system/develop DeepWork workflow"

dependencies:
  tools:
    - type: "mcp"
      value: "deepwork"
      description: "DeepWork MCP server"
